.wrapper
  .side-bar
    .side-bar__header
      .side-bar__header--box
        .side-bar__header--box--name
          = current_user.name
        %ul.side-bar__header--box--lists
          %li.list
            = link_to do
              %i.fa.fa-edit
          %li.list
            = link_to edit_user_path(current_user) do
              %i.fa.fa-cog
    .groups
      .groups__main
        = link_to "/groups/34402/messages" do
          .groups__main--name
            uuu
          .groups__main--message
            まだメッセージはありません。
